import { IoPersonOutline } from "react-icons/io5";

const Btn = () => {
  return (
    <button className="linksBtn" aria-label="sign up/in">
      <IoPersonOutline fontSize={25} />
    </button>
  );
};

export default Btn;
